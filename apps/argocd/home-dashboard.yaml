---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: home-dashboard
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/milanoid-labs/homelab-cluster.git
    targetRevision: HEAD
    path: apps/argocd/home-dashboard
  destination:
    server: https://kubernetes.default.svc
    namespace: home-dashboard
  syncPolicy:
    automated:
      enabled: true
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
